const SelectField = ({ name, selectOptions }) => {
  return {
    /* <div className="mt-3">
            <select name={} >
                {selectOptions.map(item)=>(
                    <>
                    <option value={item} key={item}>{item}</option>
                    </>
                )}
            </select>
        </div> */
  };
};

export default SelectField;
